# cactusapi 0.1.1 (2023-02-27)
* `get_representation` now correctly returns the XML string for non-200 status
  codes when `xml = TRUE`.

# cactusapi 0.1.0

* Added a `NEWS.md` file to track changes to the package.
